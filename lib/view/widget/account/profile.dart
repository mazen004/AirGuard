import 'package:flutter/material.dart';

class ProfileCustom extends StatelessWidget {
  const ProfileCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}